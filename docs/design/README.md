# Design documents

Use this directory only for changes that require multiple implementation packages or durable product/architecture decisions. Small changes and single-package work belong in `docs/handoff/`.
